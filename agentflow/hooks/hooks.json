{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f .claude/agentflow.yaml ] && [ -z \"${GITHUB_TOKEN:-}\" ]; then echo '[agentflow] GITHUB_TOKEN not set — GitHub MCP calls will fail' >&2; fi"
          }
        ]
      }
    ]
  }
}
